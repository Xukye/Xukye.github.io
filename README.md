# Yao Zhuang — Personal Website

Static academic profile published with GitHub Pages.

Edit profile and CV information in `content/`. Styling is in `academic.css` and rendering is in `academic.js`.

Preview locally with `python3 -m http.server 4174`, then open http://localhost:4174/.
